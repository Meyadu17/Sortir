{% extends 'base.html.twig' %}

{% block titre %} {{ parent() }} | Créer{% endblock %}

{% block main %}

    <h1>Créer une sortie</h1>
    <div class="col-gauche">
        {{ form_start(sortieForm) }}
        <div class="label">
            {{ form_label(sortieForm.nom) }}</div>
        <div class="input-group">
            {{ form_widget(sortieForm.nom) }}</div>
        <div class="label">
            {{ form_label(sortieForm.date_heure_debut) }}</div>
        <div class="input-group">
            {{ form_widget(sortieForm.date_heure_debut) }}</div>
        <div class="label">
            {{ form_label(sortieForm.duree) }}</div>
        <div class="input-group">
            {{ form_widget(sortieForm.duree) }} minutes
        </div>
        <div class="label">
            {{ form_label(sortieForm.date_limite_inscription) }}</div>
        <div class="input-group">
            {{ form_widget(sortieForm.date_limite_inscription) }}</div>
        <div class="label">
            {{ form_label(sortieForm.nb_inscriptions_max) }}</div>
        <div class="input-group">
            {{ form_widget(sortieForm.nb_inscriptions_max) }}</div>
        <div class="label">
            {{ form_label(sortieForm.infos_sortie) }}</div>
        <div class="input-group">
            {{ form_widget(sortieForm.infos_sortie) }}</div>
    </div>


    <div class="col-droite">
        {{ form_start(lieuForm) }}
{#        <div class="label">#}
{#            {{ form_label(lieuForm.SITES) }}</div>#}
{#        <div class="input-group">#}
{#            {{ form_widget(lieuForm.SITES) }}</div>#}
        <div class="label">
            {{ form_label(lieuForm.villes) }}</div>
        <div class="input-group">
            {{ form_widget(lieuForm.villes) }}</div>
        <div class="label">
            {{ form_label(lieuForm.nom) }}</div>
        <div class="input-group">
            {{ form_widget(lieuForm.nom) }}</div>
        <div class="label">
            {{ form_label(lieuForm.rue) }}</div>
        <div class="input-group">
            {{ form_widget(lieuForm.rue) }}</div>
{#        <div class="label">#}
{#            {{ form_label(lieuForm.CODEPOSTAL) }}</div>#}
{#        <div class="input-group">#}
{#            {{ form_widget(lieuForm.CODEPOSTAL) }}</div>#}
        <div class="label">
            {{ form_label(lieuForm.latitude) }}</div>
        <div class="input-group">
            {{ form_widget(lieuForm.latitude) }}</div>
        <div class="label">
            {{ form_label(lieuForm.longitude) }}</div>
        <div class="input-group">
            {{ form_widget(lieuForm.longitude) }}</div>
    </div>
{% endblock %}
